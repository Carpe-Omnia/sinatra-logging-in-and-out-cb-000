<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <p>god damn it</p>
    <% if Helpers.is_logged_in?(@sesh) %>
      <% user = Helpers.current_user(@sesh) %>
      <h1><%= "Welcome #{user.username}" %></h1><br>
      <p> Password: <%= user.password %> </p><br>
      <h3> Your Balance: <%= "#{user.balance}.0"%> </h3><br>
    <% end %>
  <a href="/logout" name="Log Out">Log Out </a>
  </body>
</html>
